#include <iostream>
#include "building.hpp"
#include "house.hpp"
#include "office.hpp"
#include "library.hpp"

int main() {
    /* INSTRUCTIONS:
    For each function, do the following:
    1. Uncomment the constructor call and print_info()
    2. Implement these functions. The print call will be up to you, but ensure that the necessary info is displayed
    and that the reader can tell what the numbers/bools/names represent.
    3. (EXTRA) if you have time, implement the get_type() call. What type of function is this? 
    */

    // // Create House object
    // House house("Sunny Villa", 2, 3, true);
    // house.print_info();
    // std::cout << "Type: " << house.get_type() << std::endl;

    // std::cout << std::endl;

    // // Create Office object
    // Office office("Tech Corp", 5, 50, true);
    // office.print_info();
    // std::cout << "Type: " << office.get_type() << std::endl;

    // std::cout << std::endl;

    // // Create Library object
    // Library library("City Library", 3, 10000, true);
    // library.print_info();
    // std::cout << "Type: " << library.get_type() << std::endl;

    return 0;
}