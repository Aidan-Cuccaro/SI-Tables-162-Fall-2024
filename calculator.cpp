/* INSTRUCTIONS: Calculator app
 * Write a template class called Caluclator that does the four basic operations (+-/*) on
 * ints, floats, and doubles. Main has been provided for you, all you need to do is create the class.
 * There is no need to store variables as they will be passed directly in, all you need is to create
 * the functions.
*/

#include <iostream>











int main() {
    // UNCOMMENT EACH LINE WHEN YOU FINISH THAT FUNCTION TO TEST IT
    // Calculator<int> intCalc;
    // std::cout << "Integer Operations:" << std::endl;
    // std::cout << "5 + 3 = " << intCalc.add(5, 3) << std::endl;
    // std::cout << "5 - 3 = " << intCalc.subtract(5, 3) << std::endl;
    // std::cout << "5 * 3 = " << intCalc.multiply(5, 3) << std::endl;
    // std::cout << "5 / 3 = " << intCalc.divide(5, 3) << std::endl;
    // std::cout << "5 / 0 = " << intCalc.divide(5, 0) << std::endl;

    // Calculator<double> doubleCalc;
    // std::cout << "\nDouble Operations:" << std::endl;
    // std::cout << "5.5 + 3.2 = " << doubleCalc.add(5.5, 3.2) << std::endl;
    // std::cout << "5.5 - 3.2 = " << doubleCalc.subtract(5.5, 3.2) << std::endl;
    // std::cout << "5.5 * 3.2 = " << doubleCalc.multiply(5.5, 3.2) << std::endl;
    // std::cout << "5.5 / 3.2 = " << doubleCalc.divide(5.5, 3.2) << std::endl;
    // std::cout << "5.5 / 0 = " << doubleCalc.divide(5.5, 0) << std::endl;

    return 0;
}